# Dependencies
node_modules/
# 参考
参考/
#test (root scratch only; renderer vitest setup lives under src/renderer/src/test/)
test/
!src/renderer/src/test/
!src/renderer/src/test/**
# Build / pack output (regenerate: npm run build, npm run icon:export)
out/
dist/
build/icon.png
build/icon.ico

# Local-only docs (dev notes, audits, archives) — publishable docs stay in doc/
docs/

# Root-level audit dumps (canonical copy under docs/audit/)
audit-report-*.md
audit-report-*.html

# Local package archives
pi-goal-*.tgz

# Logs & debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db
desktop.ini
nul

# Env & secrets
.env
.env.*
!.env.example

# Agent / local assistant instructions (本机约定，不进仓库)
AGENTS.md

# IDE / editor local (not shipped)
.cursor/
.idea/
*.swp
*.swo
*~

# Project-local pi + Trellis (machine / workflow, not app source)
# Never git add -f .trellis — tracked files ignore this rule
.pi/
.trellis/

# Understand-Anything knowledge graph (local analysis artifact, not app source)
.ua/

# Generated / scratch / test artifacts
.image-gen/
screenshot*.png
screenshot*.jpg
screenshot*.jpeg
*-result.txt
*.tmp
*.temp
.cache/
coverage/
.nyc_output/
test-results/
playwright-report/
playwright/.cache/
tmp/
temp/

# Electron / Vite
*.tsbuildinfo
.vite/

# Native addons rebuild noise (keep package-lock)
*.node.map

# Local-only git history rewrite helpers (do not publish)
scripts/filter_repo_message*.py
scripts/replace-text-history.txt
scripts/rewrite-commit-messages.txt
scripts/fix-arrow-syntax.py*.tgz
scripts/git-rewrite-public-main.ps1
scripts/git-rewrite-public-main.sh

# Local machine artifacts (本机配置与残留，不进仓库)
opencode.json
# Root-level stray build output (out/ is ignored; a copy escaped to repo root)
/index.js
# WSL mount residue (本地产物，勿提交；目录名以反斜杠开头，git 不解析转义，
# 用通配匹配 UNC 前缀)
*wsl.localhost*
